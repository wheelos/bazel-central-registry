diff --git include/fastdds/rtps/writer/RTPSWriter.h include/fastdds/rtps/writer/RTPSWriter.h
index b4503bd..94eaa62 100644
--- include/fastdds/rtps/writer/RTPSWriter.h
+++ include/fastdds/rtps/writer/RTPSWriter.h
@@ -20,6 +20,7 @@
 #define _FASTDDS_RTPS_RTPSWRITER_H_

 #include <chrono>
+#include <atomic>
 #include <functional>
 #include <limits>
 #include <memory>
@@ -549,6 +550,7 @@ protected:
     bool is_async_ = false;
     //!Separate sending activated
     bool m_separateSendingEnabled = false;
+    std::atomic<bool> deinitialized_{false};

     //! The liveliness kind of this writer
     LivelinessQosPolicyKind liveliness_kind_;
diff --git include/fastdds/rtps/writer/StatefulWriter.h include/fastdds/rtps/writer/StatefulWriter.h
index 0b45a7b..afb39c0 100644
--- include/fastdds/rtps/writer/StatefulWriter.h
+++ include/fastdds/rtps/writer/StatefulWriter.h
@@ -332,6 +332,8 @@ public:

     SequenceNumber_t next_sequence_number() const;

+    void begin_shutdown();
+
     /**
      * @brief Sends a periodic heartbeat
      *
@@ -523,6 +525,7 @@ private:

     //! True to disable piggyback heartbeats
     bool disable_heartbeat_piggyback_;
+    std::atomic<bool> shutting_down_{false};
     //! True to disable positive ACKs
     bool disable_positive_acks_;
     //! Keep duration for disable positive ACKs QoS
diff --git src/cpp/fastrtps_deprecated/publisher/PublisherImpl.cpp src/cpp/fastrtps_deprecated/publisher/PublisherImpl.cpp
index f4b17fb..ffbf936 100644
--- src/cpp/fastrtps_deprecated/publisher/PublisherImpl.cpp
+++ src/cpp/fastrtps_deprecated/publisher/PublisherImpl.cpp
@@ -103,6 +103,10 @@ PublisherImpl::~PublisherImpl()
     if (mp_writer != nullptr)
     {
         EPROSIMA_LOG_INFO(PUBLISHER, this->getGuid().entityId << " in topic: " << this->m_att.topic.topicName);
+        if (auto* writer = dynamic_cast<StatefulWriter*>(mp_writer); writer != nullptr)
+        {
+            writer->begin_shutdown();
+        }
     }

     RTPSDomain::removeRTPSWriter(mp_writer);
diff --git src/cpp/rtps/writer/RTPSWriter.cpp src/cpp/rtps/writer/RTPSWriter.cpp
index 9deb4ce..028f391 100644
--- src/cpp/rtps/writer/RTPSWriter.cpp
+++ src/cpp/rtps/writer/RTPSWriter.cpp
@@ -162,6 +162,11 @@ RTPSWriter::~RTPSWriter()

 void RTPSWriter::deinit()
 {
+    if (deinitialized_.exchange(true))
+    {
+        return;
+    }
+
     // First, unregister changes from FlowController. This action must be protected.
     {
         std::lock_guard<RecursiveTimedMutex> guard(mp_mutex);
diff --git src/cpp/rtps/writer/StatefulWriter.cpp src/cpp/rtps/writer/StatefulWriter.cpp
index 596a8a2..2e4c79d 100644
--- src/cpp/rtps/writer/StatefulWriter.cpp
+++ src/cpp/rtps/writer/StatefulWriter.cpp
@@ -1733,6 +1733,15 @@ SequenceNumber_t StatefulWriter::next_sequence_number() const
     return mp_history->next_sequence_number();
 }

+void StatefulWriter::begin_shutdown()
+{
+    {
+        std::unique_lock<RecursiveTimedMutex> lock(mp_mutex);
+        shutting_down_.store(true);
+    }
+    deinit();
+}
+
 bool StatefulWriter::send_periodic_heartbeat(
         bool final,
         bool liveliness)
@@ -1999,6 +2008,11 @@ bool StatefulWriter::process_acknack(
         fastdds::rtps::VendorId_t /*origin_vendor_id*/)
 {
     std::unique_lock<RecursiveTimedMutex> lock(mp_mutex);
+    if (shutting_down_.load())
+    {
+        result = false;
+        return false;
+    }
     result = (m_guid == writer_guid);

     if (result)
